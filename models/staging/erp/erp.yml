sources:
  - name: erp
    description: Sistema de vendas Northwind
    schema: raw_northwind
    database: fea24_11
    tables:
      - name: category
        columns:
          - name: id
            tests:
              - unique
              - not_null
      - name: products
        columns:
          - name: id
            tests:
              - unique
              - not_null
      - name: suppliers
        columns:
          - name: id
            tests:
              - unique
              - not_null